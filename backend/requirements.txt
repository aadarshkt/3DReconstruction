# ── Web framework & async ─────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9         # multipart file upload support
websockets==12.0

# ── Task queue ────────────────────────────────────────────────────────────────
celery==5.4.0
redis==5.0.4                    # Celery broker + result backend

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.30
asyncpg==0.29.0                 # async PostgreSQL driver
alembic==1.13.1                 # database migrations
psycopg2-binary==2.9.9          # sync driver for Celery tasks
greenlet==3.5.5                 # required for asyncpg with SQLAlchemy

# ── 3D processing ─────────────────────────────────────────────────────────────
open3d>=0.18.0,<=0.19.0
numpy==1.26.4
scipy==1.13.1                   # RANSAC line fitting, geometry helpers

# ── CAD / drawing export ──────────────────────────────────────────────────────
ezdxf==1.3.4
svgwrite==1.4.3

# ── Image / video utilities ───────────────────────────────────────────────────
Pillow==10.3.0                  # image metadata / preview thumbnails

# ── Utilities ─────────────────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.1
python-dotenv==1.0.1
aiofiles==23.2.1                # async file I/O
structlog==24.1.0               # structured logging
pyjwt>=2.8.0                    # JWT tokens for OAuth & RBAC

# ── Validation / testing ──────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7

# ── Policy RAG & Agent Orchestration ─────────────────────────────────────────
pdfplumber>=0.11.0
chromadb>=0.5.0
httpx>=0.27.0

